export const container = () => ({
    border: 1,
});
export const item = () => ({
    marginTop: '0.5rem',
    marginBottom: '0.5rem',
    width: '100%',
});

export const empty = ({ theme }) => ({
    margin: '0 auto',
    color: theme.colors.secondaryText,
});
