import React from 'react';
import { FiltersFetcher, FilterBar } from 'modules/filters';
const Filters = () => (
    <FiltersFetcher>
        <FilterBar />
    </FiltersFetcher>
);

export default Filters;
