const config = {
    api: {
        base: 'https://skeleton',
    },
    // TODO: add appUrl
    // appUrl: isEnvProduction ? 'http://localhost:3000' : `https://api-${appName}-production.ack.ee`,
};

export default config;
